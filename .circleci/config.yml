# CircleCI configuration file

version: 2.1
jobs:
  build:
    docker:
      - image: docker:stable
    steps:
      - checkout
      - run:
          name: Build Docker image
          command: |
            docker build -t oc-lettings-site .
