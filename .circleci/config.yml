# CircleCI configuration file

version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:2023.07
    steps:
      - checkout
    
      - run:
          name: read env
          command: |
            echo "Commit Hash: $CIRCLE_SHA1"
